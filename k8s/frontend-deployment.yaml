apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: jonny
  labels:
    app: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
<<<<<<< HEAD
        image: ghcr.io/jeeonpark/frontend:323c524d
=======
        image: ghcr.io/jeeonpark/frontend:323c524d
>>>>>>> c633e2d (fix: update deployment configurations)
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5 
